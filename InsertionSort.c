#include <stdio.h>
#define Swap(x,y) (x!=y) && (x ^= y ^= x ^= y)
void OutPut(int Arr[],int len) {
    for(int i = 0; i < len; i++) 
        printf("%d ",Arr[i]);
    printf("\n");
    }
void InsertionSort(){
    
}
int main () {

    return 0;
}